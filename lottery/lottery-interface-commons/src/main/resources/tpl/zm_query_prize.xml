<?xml version="1.0" encoding="utf-8"?>
<msg>
  <head transcode="${transcode}" partnerid="${partnerid}" version="${version}" time="${time}" />
  <body>
    <queryprize lotteryId="${lotteryId}" issueNumber="${issueNumber}" prevTicketId="$!{prevTicketId}" status="${status}"/>
  </body>
</msg>